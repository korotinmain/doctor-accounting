rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /visits/{visitId} {
      // MVP mode: allow app access without Firebase Auth.
      // Tighten this rule before public launch.
      allow read, write: if true;
    }
  }
}
